# 异世界乙女向 AI 互动叙事产品 · 设计方案 v1

> 本文档用于网页体验版开发参考。重点是把产品的交互语言、视觉气质、状态机器跑通,不是完整游戏。剧情内容、AI 接入、立绘资源等可以用占位 / 模拟数据代替。

---

## 1. 产品定位

异世界乙女向 AI 互动叙事产品。多角色单推,每个角色有独立时空(古代 / 现代 / 未来风)。用户用语音(主推)或文字与角色对话推动剧情,AI 导演规划用户在大节点之间的小走向。

无"通关"概念,核心乐趣是推进过程的拉扯感和自由生成感。强配合的用户走主线,弱配合的用户在大世界探索中也能找到乐趣。

目标用户:对剧情和文字本身有强代入感的人,而不是靠卡面驱动的视觉派。

---

## 2. 五条设计原则

1. **一切 UI 都是世界元素,不是界面元素** — 不是"录音按钮",是"会呼吸的圆";不是"通知红点",是"余光里多出的一颗光"
2. **手势承载情感强度** — 点按 = 注意,长按 = 拥抱,滑动 = 行动,松手 = 放下
3. **余光设计** — 非主体信息从屏幕边缘渗入,绝不打断中心叙事
4. **时间真实** — 角色离开就是离开,世界用世界的语言说话,没有 loading spinner
5. **先奖励、后足迹** — 早期获得感主导,后期回望感主导,是两套语言

---

## 3. 主界面

### 3.1 版式

- **横屏** (landscape only)
- **背景色**:`#0c0814` (近黑暗紫)
- **比例**:16:9 内容区

### 3.2 核心元素布局

```
┌─────────────────────────────────────────────────┐
│ ⊙ 历史锚点                          ··· ◉ 微痕  │
│ (左上,极小)                          (右上,余光)│
│                                                 │
│         ┌──┐                                    │
│         │  │     ✿                              │
│         │立│                                    │
│         │绘│                       ·             │
│         │  │            ✿                       │
│         └──┘                                    │
│                                                 │
│                                          ╲      │
│                                       长按·说话 │
│                                            ◯    │
└─────────────────────────────────────────────────┘
                                      (右下,半隐)
```

| 元素 | 位置 | 说明 |
|---|---|---|
| 角色立绘 | 居中偏左(left ≈ 22%,垂直接近满高) | 会呼吸,承担 90% 视觉信息,周围**无任何 UI 浮窗** |
| 美术单体 | 中段偏右 | 稀疏漂浮(一片叶、一颗光点),临近大节点逐步积累 |
| 呼吸圆环 | 右下角(半隐于画外) | 默认缓慢呼吸,长按说话,详见 §4 |
| 历史锚点 | 左上角 | 极小的视觉锚(墨痕 / 光点),拇指滑动可拉开事记 |
| 奖励微痕 | 右上角 | 余光浮现的暖色光点,点击触发次级界面 |
| 氛围色 + 环境声 | 全屏背景 | 跟随剧情情绪、时间光线、角色情绪变化 |

### 3.3 明确不出现的元素

- 任何聊天气泡 / 输入框 / 按钮图标
- 任何系统状态文字标识(包括"第几夜 · 哪里"这类世界时间标识)
- 任何直线 / 矩形元素(全是有机形状)
- 任何 loading spinner / 进度条 / 倒计时

### 3.4 视觉规格速查

| 项 | 值 |
|---|---|
| 主背景色 | `#0c0814` |
| 立绘呼吸周期 | 4-5s |
| 美术单体漂浮周期 | 6-9s,各元素错开 |
| 字体 · 角色对话字幕 | 衬线(思源宋体 / Source Han Serif) |
| 字体 · 旁白 | 衬线斜体 |
| 字体 · 系统标签 | 无衬线(思源黑体 / Source Han Sans) |
| 字号 · 对话字幕 | 13-14 px |
| 字号 · 旁白 | 12 px |
| 主要圆角 | 全有机形状,不使用矩形 |

---

## 4. 输入系统 · 圆环六态

呼吸圆环位于右下角,默认半隐于画外。所有状态如下:

| 状态 | 触发 | 视觉表现 | 时长 / 节奏 |
|---|---|---|---|
| **静默** | 默认 | 半圆缓慢呼吸 | 4s 周期 |
| **收音** | 长按中 | 圆环补完成完整圆,亮度提升,内沿声波环随音量起伏,立绘微微转向用户 | 跟随用户按住 |
| **投递** | 松手瞬间 | 圆环坍缩成光点,飘向角色立绘方向被她"接住"(立绘可能轻微一震或眼神接住) | 1-1.5s |
| **静思** | 角色处理中 | 圆环退到最暗,呼吸放慢到 7-8s,画面其他元素静下,立绘做思考动作(微低头、目光偏远) | 不定,在意不在快 |
| **回应** | 角色说话中 | 圆环近乎消失,立绘做嘴型 + 表情 + 姿态,身侧淡入字幕(衬线、逐句、可关闭) | 跟随语音长度 |
| **输入** | 短按 | 圆环不变,下方淡入一条极细的横线(墨痕),用户在墨痕上打字,衬线手写体浮现,完成后向上轻扫提交 | 跟随用户操作 |

### 4.1 邀请态(附加)

用户超过 30 秒未动作,圆环做一次明显呼吸(亮度比平常更深),不弹任何文字。再久一些,角色立绘自发做小动作(歪头、看远方),主动给"她还活着"的信号。

### 4.2 实现提示

- 收音态的声波环可用 Web Audio API 的 AnalyserNode 取实时音量驱动 SVG / Canvas
- 投递态的光点轨迹可用 CSS keyframes 或 GSAP MotionPath
- 输入态的"墨痕"是一条 1-2 px 高度的极细横线,文字以衬线字体在其上方浮现,提交后线条收回

---

## 5. 叙事信息五通道

不同类型信息走不同视觉通道,互不冲突。

### 通道一 · 角色对话
- 听觉(语音)为主,视觉辅助
- 立绘嘴型 + 表情 + 姿态变化
- 身侧字幕(衬线、逐句淡入淡出、可关闭)
- **绝不用聊天气泡**

### 通道二 · 角色动作
- **可演的**(动手、转身、靠近):立绘自己演,不需要文字
- **演不了的**(走到山上、打开抽屉):屏幕**固定边缘位置**浮现一句旁白,衬线斜体,极淡灰白,3-4 秒淡入淡出。位置固定后,用户会形成"那里是世界在说话"的认知

### 通道三 · 场景变化
- **微变**(光线、时间、心境):背景色 / 美术单体悄悄替换,渐变
- **大变**(地点、跳过时间):**眨眼过渡** — 屏幕暗下 0.8s 像合眼,睁开后已是新场景。比黑屏过场更轻

### 通道四 · 过场 / 大剧情节点
- 临近大节点时,新美术单体逐个加入,既有的不消失
- 直到屏幕变成相对完整的场景画面 — 这本身就是奖励
- 此时一个明显的发光元素出现,长按 / 点击 = 触发次级界面(剧情视频片段、记忆点等)
- 看完次级界面后回到稀疏的主界面,**而不是积累的画面** — 大场景"经历过"了,留白回归

### 通道五 · NPC 对话
- **NPC 同场**:简化次要立绘出现(比主角小、不那么精致),贴 NPC 位置出现 TA 的话(用不同字体与主角文字区分)
- **NPC 缺席旁白**(隔门、隔墙):屏幕某一边缘出现文字,带"从那边来"的位置感(从右侧渐入、停留、向左飘走)
- **群体环境**(集市、宫宴):背景出现淡淡剪影群 + 局部光斑,环境声起,偶尔一两句路人语从某方位浮现又消失

---

## 6. 角色离场 · AI 导演的工具箱

离场是剧情决定的场景状态,**不是固定 UI**。AI 导演可选用以下四种处理方式之一(或组合):

1. **空位** — 她原本的位置空着,场景其余元素保留。仅在剧情需要"你独自待在这空间"时使用,空位本身就是叙事
2. **视角切换** — 画面转向用户这边的场景。**大多数情况推荐用这种**,避免 UI 停滞感
3. **时间跳过** — 眨眼过渡直接到下一个剧情时刻,她回来时画面已是新场景
4. **物件出现** — 仅当剧情真有"她把东西留给你"这条线时才用(比如她交给你信物),**不是默认陪伴物**

### 6.1 时间感处理
- 默认不显示文字时间标识
- 仅在剧情明确强调"时间流逝"时,用环境光线 / 美术单体 / 角色姿态来表达
- 基本不动用文字标识

---

## 7. 历史事记

### 7.1 漂流机制(信息发生时)

每条信息履行完功能后,都向左上方缩小、淡化、流走:

- 用户的话:墨痕处 → 化作光点投递给角色 → 光点轨迹延伸,继续向左上飞去淡入历史
- 角色的话:身侧字幕 → 字幕收拢成一行,向左上漂走
- 旁白:屏幕边缘文字 → 向左上飘走
- NPC 对话:从 NPC 位置漂出 → 也归向左上

最终形成"所有过去都在向左上方汇聚"的视觉。这是产品时间感的核心动作语言。

### 7.2 召回机制(用户拉开时)

- 左上角始终留一个极小的视觉锚(墨痕 / 光点 / 光晕)
- 左手大拇指从左上向内滑动 → 一卷事记展开覆盖屏幕
- 半透明叠在当前场景上,场景轻微暗下
- 滚动浏览,反向滑动收回
- 展开和收回都带方向感,**不是简单的开关**,保持空间连续

### 7.3 条目类型(只存文字)

仅两类条目 + 一种章节级强调:

| 类型 | 内容 | 视觉 |
|---|---|---|
| 对话条目 | 角色或用户的话 | 衬线,不同人用字体或缩进区分,带极小的"她 / 你"标签 |
| 旁白条目 | 动作 / 场景的舞台指示 | 衬线斜体,淡色,缩进 |
| 重旁白(章节级) | 大节点高光时刻 | 前后加墨横线,文字稍长带场景描写 |

重旁白示例:
```
── ── ── ── ── ──
渡口的灯一盏接一盏亮起,她回头看你
── ── ── ── ── ──
```

### 7.4 视觉事件处理规则

- 立绘微动作(眨眼、转头):**不进历史**,留给当下感受
- 有意义的角色动作 / 场景变化:本来就有旁白浮现,旁白本身进历史
- 大节点高光:用重旁白记录,**不做截图也不做 AI 生成速写**

### 7.5 事记视觉规格

| 项 | 值 |
|---|---|
| 纸面背景 | `rgba(238,228,210,0.96)` (暖米色) |
| 主文字色 | `#2a2218` |
| "她 / 你"标签色 | `#6e5b3a` / `#8a7245` |
| 旁白斜体色 | `#75603e` |
| 墨横线 | 由若干 ── 等字符组成,与正文同色但更淡 |

### 7.6 用户能做的 / 不能做的

- 用户**不能**编辑事记(不能标记重要、不能加备注)
- 事记当前**只存文字**,无图片 / 速写 / 截图

---

## 8. 手势词典

| 手势 | 行为 |
|---|---|
| 长按右下圆环 | 语音输入 |
| 短按右下圆环 | 文字输入 |
| 文字输入后上滑 | 提交 |
| 左手左上向内滑动 | 拉开事记 |
| 左手反向(向左上)滑动 | 收回事记 |
| 长按物件 | 触发对应的内心旁白(剧情触发时才有) |
| 点击美术单体浮现的发光元素 | 触发次级界面 |

### 8.1 网页版实现提示

- 长按检测:`mousedown / touchstart` + 250ms 延迟阈值
- 滑动检测:`pointerdown → pointermove → pointerup` 序列,记录方向向量
- 网页版可考虑保留 `keyboard` 兼容(空格 = 长按,Enter = 提交)作为开发期调试

---

## 9. 奖励与次级界面

### 9.1 早期(获得感)
- 每次推进给具体可触摸的"小光点"奖励
- 光点出现在余光区域(右上、画面边缘),不打断主界面
- 用户主动点击 → 触发次级界面

### 9.2 大节点(高光)
- 美术单体积累成完整场景
- 触发次级界面:剧情视频片段 / 记忆点 / 关键剧情展现
- 次级界面视觉**更丰富**(可以有完整美术、动效、音乐),与主界面留白形成对比
- 看完即消失,回到稀疏的主界面,**不保留积累的画面**

### 9.3 后期(回望感)
- 这些光点会连成"足迹" / 旅程图谱
- 具体视觉语言后续设计(暂不在 v1 范围)

---

## 10. 明确不做的事

- ❌ 任何聊天气泡 / 输入框 / 按钮图标
- ❌ 任何系统状态文字(等待倒计时、进度条、加载动画)
- ❌ 默认陪伴物 / 信使元素(只在剧情真需要时才出现)
- ❌ AI 生成的速写 / 截图 / 缩略图条目
- ❌ 黑屏过场(用眨眼过渡代替)
- ❌ 用户编辑事记
- ❌ 关系网络 UI(单推,暂不需要)
- ❌ 直线 / 矩形元素(全有机形状)
- ❌ 文字时间标识 / 世界时间显示(默认隐藏)

---

## 11. 网页体验版建议范围

为了在合理工程量内验证交互语言,网页版建议聚焦于:

### 必做(核心体验)
1. 横屏主界面 + 立绘呼吸 + 美术单体漂浮 + 氛围背景
2. 圆环六态完整状态机(用模拟语音输入即可,Web Audio API 取音量驱动声波环)
3. 文字输入态(墨痕 + 衬线打字 + 上滑提交)
4. 字幕系统(角色对话身侧字幕,逐句淡入淡出)
5. 旁白系统(屏幕边缘浮现,3-4s 淡入淡出)
6. 漂流动画(信息向左上方流走)
7. 事记展开 / 收回(左上滑入)
8. 一段预设的对话剧情走完整流程(可以全部预设,不接 AI)

### 可做(锦上添花)
- 美术单体积累成完整场景 → 触发次级界面 demo
- 眨眼过渡(场景大变切换)
- 邀请态(用户长时间不动作,立绘自发动作)
- NPC 旁白文字(从某侧渐入)

### 可不做(v1 范围外)
- 真实 AI 接入(可全部预设剧本)
- 真实立绘(可用占位 SVG / 简单形状代替)
- 多角色切换
- 关系网 / 记忆星图等次级界面
- 完整次级界面内容
- 用户身份 / 登录 / 数据持久化

---

## 12. 待解决 / 后续讨论(v1 之后)

- 新手第一次打开 APP 的 60 秒 — 怎么用产品本身教会用户这套手势和留白语言,不靠 tutorial
- 立绘演技清单 — 给美术列出立绘必须能做的动作类型
- AI 导演与小走向的具体落地 — 大节点之间的小走向怎么生成、衔接
- 次级界面的视觉语言 — 剧情视频、记忆点、足迹图谱等
- 第二、三个角色加入后 — 怎么切换 / 进入不同角色的世界
- 声音音效 — 氛围音、环境声、立绘动作音的整体设计
- 事记长线节奏 — 无限累积还是分册

---

## 附录 · 视觉规格汇总(开发用)

### 颜色

| 用途 | 值 |
|---|---|
| 主背景 | `#0c0814` |
| 立绘高光参考 | `rgba(255,225,200,0.18-0.4)` |
| 立绘衣物参考 | `rgba(180,160,210,0.13-0.25)` |
| 圆环描边 | `rgba(255,255,255,0.35)` 静默 / `0.7-0.9` 收音 |
| 美术单体 · 叶 | `rgba(170,210,160,0.3-0.5)` |
| 美术单体 · 光点 | `rgba(220,200,255,0.5-0.85)` |
| 奖励微痕 | `rgba(255,200,150,0.8-1)` |
| 字幕文字 | `rgba(255,255,255,0.85)` |
| 旁白文字 | `rgba(255,255,255,0.4)` |
| 事记纸面 | `rgba(238,228,210,0.96)` |
| 事记主文字 | `#2a2218` |
| 事记标签 | `#6e5b3a` / `#8a7245` |

### 动画时长

| 项 | 时长 |
|---|---|
| 立绘呼吸 | 4-5s |
| 圆环呼吸 (静默) | 4s |
| 圆环呼吸 (静思) | 7-8s |
| 收音光环脉动 | 0.8-1s |
| 投递光点动画 | 1-1.5s |
| 旁白淡入淡出停留 | 3-4s |
| 眨眼过渡 | 0.8s |
| 美术单体漂浮 | 6-9s |
| 邀请态触发阈值 | 30s 无操作 |
| 事记展开 / 收回 | 0.4-0.6s |

### 字体推荐

| 用途 | 字体 |
|---|---|
| 角色对话字幕 | `'Source Han Serif SC', 'Songti SC', serif` |
| 旁白(斜体) | 同上,`font-style: italic` |
| 用户输入(墨痕区) | `'Source Han Serif SC', serif`(可加 `letter-spacing` 增加手写感) |
| 系统标签 / UI | `'Source Han Sans SC', 'PingFang SC', sans-serif` |

### 缓动函数

- 圆环呼吸:`ease-in-out`
- 漂流到左上:`cubic-bezier(0.4, 0, 0.2, 1)` (类似 Material standard easing)
- 眨眼过渡:`ease-in` 暗下 + `ease-out` 醒来
- 事记展开:`cubic-bezier(0.32, 0.72, 0, 1)` (iOS 弹性感)

---

## 文档变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1 | 2026-04-27 | 初版,覆盖主界面、输入系统、五通道、离场工具箱、事记、手势、视觉规格 |
