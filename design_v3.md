# 设计方案 v3

v2 已经把"幕"的整体感落地了。这一版接下来要解决三件事:**立绘要更有存在感**,**几层文字要有统一语言**,**剧本要让对话驱动占大头**(留作下一步)。

---

## 一、立绘放大,主露上半身

现在立绘按 contain 缩在 60vh 的方框里,显得小、漂浮。

调整为:**画面下方做底,下半身可以延伸到屏幕之外**。让她真正"在场"。

```
   ┌────────────────────────────────┐
   │                                 │
   │                                 │
   │       ╭─────╮                  │
   │       │ 上 │   (主露上半身)    │
   │       │ 半 │                   │
   │       │ 身 │                   │
   │       ├─────┤                  │  ← 屏幕下边
   │       │ 下 │   (延伸出屏外)   │
   │       └─────┘                  │
```

仍然保留"立绘自由位置"这件事——剧情里随时可以推近成特写、拉远成远景、或挪到画面其他位置。**这只是默认值**。

---

## 二、两套文字语言

现在屏幕上有六种文字(用户/角色对话、旁白、内心、重旁白、NPC),各自字体、字号、颜色、位置都不同——看起来像六个产品。

新方案把它们收成两套:

### 第一套 · 说话(你 ↔ 她)

你和她的对话**视觉上完全对等**,只有方向相反。

| 项 | 你 | 她 |
|---|---|---|
| 字体 / 字号 / 颜色 | 衬线 / 17px / 暖白 | 同 |
| 入场 | 打字机逐字浮现 | 打字机逐字浮现(同) |
| 离场 | 停一停 → 轻轻向上散 | 同 |
| 锚点 | 屏幕右侧(你按弧的高度) | 立绘的左肩侧 |
| **展开方向** | **右锚定,向左延伸** | **左锚定,向右延伸** |

```
    她说:                            你说:
    ╭──╮                                          
    │她│ 腥臭…                                  
    ╰──╯ 杂质太多了 →                          
                                        ← 等一下…!
                                        ────── 幕
```

整个屏幕看起来就是一来一回的对位。

### 第二套 · 世界的声音(旁白 / 内心 / 重旁白 / NPC)

这四种现在各自一套样式。统一为:**字体、字号、颜色、字距全部相同**,只有位置和入场方向不同。

| 项 | 值 |
|---|---|
| 字体 | 衬线斜体 |
| 字号 | 14px |
| 颜色 | 白 50% |
| 字距 | 0.14em |
| 离场 | 轻轻向上散(同) |

四种之间的最小差异:

| 类型 | 区别 |
|---|---|
| 旁白 | 中下,无标记 |
| 内心 | 中下(同位置),`(...)` 包起来 |
| 重旁白 | 中央,前后加 ── 横线(其他不变,**不再加大字号**) |
| NPC | 中下(同),从某方向淡入淡出 |

→ 内心和旁白几乎一样,**只靠括号区分**。
→ 重旁白靠**墨横线**区分,不靠字号。
→ NPC 靠**入场方向**区分。

位置只有"中下"和"中央"两种。这种克制是统一感的来源。

---

## 三、两套语言之间的对比

```
   ╭─────────────────────────────────────╮
   │                                       │
   │                  [世界]               │ 14px / 斜体 / 白 50%
   │              旁白 / 内心 / NPC         │ 中下,统一字
   │                                       │
   │   [她说]                  [你说]      │ 17px / 暖白
   │   左锚定 →             ← 右锚定       │ 打字机
   │                                       │
   ╰─────────────────────────────────────╯
                  └─ 重旁白偶尔占据中央
```

第一套是**人在说话**——大、亮、打字机展开。
第二套是**世界在说话**——小、淡、淡入淡出。

不再有六种独立装扮,只有两种语言。

---

## 四、待办 · 剧本改为对话驱动 80%

现在剧本里旁白密度高,很多场景里 7 个用户输入点之间夹着大段旁白。这让"对话驱动"的感觉不强。

下一版剧本目标:
- **用户输入点显著增加**(从 7 个到至少 15+)
- **角色主动说的话也增加**
- **旁白只保留必须的场景切换 + 关键内心**(占 20%)

具体怎么剪、怎么扩,等本轮的设计稳定下来再讨论。

---

## 实现顺序

1. 立绘:CSS 默认值改成 width 78vh / bottom 偏负,主露上半身
2. 角色对话改打字机:`showDialogue` 改成逐字浮现,锚点跟立绘走
3. 用户对话样式向角色对齐(字号 17px、暖白)
4. 把 narration / inner / npc 三种文字统一到 14px / 斜体 / 白 50%
5. 重旁白去掉加大字号,只保留墨横线作为高光
6. 待你拍板后 → 重剪剧本
